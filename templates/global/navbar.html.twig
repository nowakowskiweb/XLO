<nav class="navbar navbar-expand-md bg-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="{{ path('announcement_index') }}">Ogłoszenia</a>
            </div>
            <div class="navbar-nav ms-auto">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="nav-link" href="{{ path('app_user') }}">Twoje konto</a>
                    <a class="nav-link" href="{{ path('app_logout') }}">Wyloguj</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}">Zaloguj</a>
                    <a class="nav-link" href="{{ path('app_register') }}">Załóż konto</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
