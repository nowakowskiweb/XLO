{% extends 'base.html.twig' %}

{% block title %}Hello AnnouncementController!{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Komponenty testowe</h1>

    <div class="shadow p-2 mb-3 bg-body rounded">
        <p><a href="{{ path('announcement_add') }}" class="link-primary">Primary link</a></p>
    </div>

    <p><a href="{{ path('app_2fa_enable') }}" class="link-primary">Test link</a></p>


    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <div class="shadow p-2 mb-3 bg-body rounded">
            <p class="mb-2">{{ app.user.getFirstName() }}</p>
            <p><a href="{{ path('announcement_index') }}" class="link-primary">Primary link</a></p>
        </div>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
        <div class="shadow p-2 mb-3 bg-body rounded">
        </div>
    {% endif %}

</div>
{% endblock %}
